Syntax:
  Load-immediate: li $RD,imm
  Add:            add $RD,$RS,$RT
  Subtract:       sub $RD,$RS,$RT
  Print:          print $RT
  Compare:        cmp $RS,$RT,S

  registers can be referenced by $r0 - $r3 or $0 - $3
